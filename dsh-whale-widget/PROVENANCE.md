# 素材来源与许可范围（PROVENANCE）

本仓库的**代码**按 MIT 许可（见 [`LICENSE`](LICENSE)）。`assets/` 目录下的**美术素材**（图片 / 动图 / 音效）**不在 MIT 覆盖范围内**，按下面的说明「原样提供」。

> **本版（0.3.8-rice）是本地合并版**：在 `dsh-whale-widget@0.3.7` 的基础上，
> 额外内联了 **`dsh-rice-widget@0.3.6-mod`「偷吃白饭」**（作者 **三萌MOE**，MIT）。
> 归并后的代码仍是 MIT；两边作者署名均保留。原始独立插件与其说明见 `偷吃白饭-说明.md`。

## 〇、合并来源署名

| 部分 | 来源 | 许可 |
|---|---|---|
| 鲸鱼挂件本体（余额/气泡/角色/音效/吸附…） | [MeteorNOX/DeepSeek-Balance-Whale-Widget](https://github.com/MeteorNOX/DeepSeek-Balance-Whale-Widget) | MIT |
| 「偷吃白饭」功能（宿主 14 条 `/dsh-whale/rice/*` 路由 + 内联前端面板） | `dsh-rice-widget` by **三萌MOE**（0.3.6-mod / 2026-09-19） | MIT |
| 本地「Token 用量」方框 | 本机维护者改动 | MIT |

> **两份 MIT 许可声明都随包保留**：本目录的 `LICENSE`（`Copyright (c) 2026 MeteorNOX`）
> 与 `LICENSE-dsh-rice-widget.txt`（`Copyright (c) 2026 dsh-rice-widget contributors`）。
> MIT 要求"在软件的所有副本中保留版权声明与许可声明"，因此**上传仓库 / 公开分发时两者必须都在**。
>
> ⚠️ **不要以 `dsh-whale-widget` 之名发布到 npm** —— 那个包名属于原作者。
> 若要发布请换成自己的包名，或给 `package.json` 加 `"private": true`。

## 一、许可范围

| 范围 | 许可 |
|---|---|
| `lib/`、`cordis.patch.yml`、`package.json`、文档与维护脚本 | **MIT**（见 `LICENSE`） |
| `assets/**`（图片 / 动图 / 音效） | **不适用 MIT**：由维护者提供或使用 AI 工具生成，按 **as-is** 随插件分发，仅用于运行本插件；不授予再许可，也不声明为原创作品。 |
| `art/rice.png` | 由鲸鱼插件作者绘制并**授权随包发布**（MIT 范围内）。 |
| `art/` 其余文件与 `songs/`（见下节） | **第三方游戏素材，有意不进发布包**。 |

这样划分的原因：代码可以明确授权，而美术素材的来源与权利状态往往无法百分之百举证 —— 与其给一个站不住的授权，不如如实标注范围，并承诺收到权利主张就处理（见第四节）。

## 二、逐项来源

| 文件 | 来源 / 说明 |
|---|---|
| `DSniang1.png`（鲸鱼本体，气泡由代码绘制） | **AI 工具生成**的图像，经人工挑选与裁切。生成工具与原始出处**已不可考**（文件内元数据已被剥离，见第三节）。 |
| `DSniang02.png` | 同上（备用整图，兼容旧版手动安装路径）。 |
| `DSH2.png`（README 顶部展示图） | 由维护者自行设计与排版（使用在线设计工具），2026-09 替换为清理版（已剥离全部内嵌元数据）。 |
| `rua.gif`、`bubble-petpet.gif` | 内置动图素材。 |
| `bubble-money1.gif` | 内置泡泡图（余额预警默认内容的配图）。 |
| `Ya1.mp3` / `Ya2.mp3`、`D1.mp3` / `D2.mp3` | 内置音效（按压 / 松开）。 |
| `minecraft-exp-orb.wav`、`task-end-a.wav` | 内置任务结束音（Minecraft·经验球 / 预设 A）。 |
| `art/rice.png`「偷吃白饭」的米粒图 | 由鲸鱼插件作者绘制并授权随包发布（**在 MIT 范围内**）。 |
| `art/bowl.png`、`food-peak.png`、`food-valley.png` | 「偷吃白饭」的碗与饭（两张 196×196、预先对齐）；随本地副本分发，**不进发布包**。 |
| `art/nang.weba`、`art/arrow-tick.weba`、`art/egg-1.png`、`art/egg-2.png`、`art/song.png` | 《绝地潜兵 2》等**第三方游戏素材**，取自实机；**只在本地/群里玩，不进发布包**。彩蛋图标刻意使用中性文件名（原名是 500kg / 380mm 两件武器的口径）。 |
| `songs/`（默认一首） | 群友版随包带的示例曲；**整个目录不进发布包**，用户自行放入音频即可（文件名即歌名，只认这一层）。 |

> ⚠️ **发布白名单是刻意收窄的**：`package.json` 的 `files` 里 `art/` **只列 `art/rice.png`**。
> 因此从 npm/tarball 安装时，上述第三方素材与 `songs/` 都会缺失，前端会**静默降级为内联 SVG 兜底碗/米粒**。
> 本地以 `link:` 方式安装则功能完整。

## 三、元数据清理

图片素材曾内嵌过设计工具的 XMP 归属信息（工具名、模板名、账号 id，甚至第三方品牌名）。自 **0.3.1** 起，随包 PNG **一律剥离** `eXIf` / `iTXt` / `tEXt` / `zTXt` 元数据块，只保留渲染必需的 `IHDR` / `sRGB` / `gAMA` / `pHYs` / `IDAT` / `IEND`。

- 复核：解析每个 PNG 的块序列，确认不再有文本 / EXIF 块；
- 剥离：逐块重写，不触碰像素数据（IHDR/IDAT/IEND 原样保留），因此画面完全不变。

## 四、权利主张 / Takedown

如果你认为 `assets/` 中有素材侵犯了你的权利，请在本仓库开一条 issue，说明**文件名**与**依据**，我们会在核实后**立即替换或移除**，不附加其它条件。我们也欢迎直接提供可自由再分发的替代素材。


图片素材曾内嵌过设计工具的 XMP 归属信息（工具名、模板名、账号 id，甚至第三方品牌名）。自 **0.3.1** 起，随包 PNG **一律剥离** `eXIf` / `iTXt` / `tEXt` / `zTXt` 元数据块，只保留渲染必需的 `IHDR` / `sRGB` / `gAMA` / `pHYs` / `IDAT` / `IEND`。

- 复核：解析每个 PNG 的块序列，确认不再有文本 / EXIF 块；
- 剥离：逐块重写，不触碰像素数据（IHDR/IDAT/IEND 原样保留），因此画面完全不变。

## 四、权利主张 / Takedown

如果你认为 `assets/` 中有素材侵犯了你的权利，请在本仓库开一条 issue，说明**文件名**与**依据**，我们会在核实后**立即替换或移除**，不附加其它条件。我们也欢迎直接提供可自由再分发的替代素材。
