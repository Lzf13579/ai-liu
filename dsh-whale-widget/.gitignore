# ─────────────────────────────────────────────────────────────────────────────
# 上传仓库专用：本目录既能当本地插件用，也可能被推成公开仓库。
# 下面这些**不允许进公开仓库**（第三方游戏素材 / 群友素材），但本地必须留着才功能完整。
# ─────────────────────────────────────────────────────────────────────────────

# 第三方游戏素材（《绝地潜兵 2》等，来源方刻意不随包发布）
art/*
!art/rice.png
#   rice.png 已获鲸鱼插件作者授权随包发布（MIT 范围内），所以单独放行。

# 群友版示例歌（整个目录不进发布）
songs/

# 打包 / 安装残留
*.bak*
*.tgz
*.zip
node_modules/
.dsh-vision-router/
SHA256SUMS.txt

# 说明：
#   · 本仓库**只提交 `package.json` files 白名单内的文件**，clone 下来即可直接使用；
#     缺少的那些素材会让前端退回内联 SVG 兜底，这是**预期行为**（见 PROVENANCE.md）。
#   · 需要含这些素材的「完整版」给别人时，请用白名单重新打包后**离线**分发 ——
#     不要把被忽略的文件提交进本仓库。
